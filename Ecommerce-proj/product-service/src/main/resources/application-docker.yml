spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://${DB_HOST:ecom-postgres-products}:${DB_PORT:5432}/${DB_NAME:products_db}}
  data:
    redis:
      host: ${REDIS_HOST:ecom-redis}
      port: ${REDIS_PORT:6379}
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:ecom-kafka:9092}